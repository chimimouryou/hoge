name: Hello World

on:
  push:
    branches:
      - main  # 'main' ブランチへのpush時に実行

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello World"
